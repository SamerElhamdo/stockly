# Stockly MCP Server - ملف الإعدادات
# انسخ هذا الملف إلى .env واملأ القيم المطلوبة

# إعدادات Django API
DJANGO_BASE_URL=http://localhost:8000
API_TOKEN=your_django_api_token_here

# إعدادات MCP Server
MCP_SERVER_PORT=3001
DEBUG=True

# إعدادات السجلات
LOG_LEVEL=INFO
LOG_FILE=mcp_server.log

# إعدادات الأداء
REQUEST_TIMEOUT=30
MAX_RETRIES=3
MAX_CONCURRENT_REQUESTS=10
CACHE_TTL=300

# إعدادات الأمان (اختياري)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000,https://stockly.encryptosystem.com
